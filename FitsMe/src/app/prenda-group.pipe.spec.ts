import { PrendaGroupPipe } from './prenda-group.pipe';

describe('PrendaGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new PrendaGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
